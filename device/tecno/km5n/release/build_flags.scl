# Device-local release flag declarations for the minimal TWRP Android 14 tree.
# Recovery does not require additional device-specific release flags.

load(
    "//build/make/core/release_config.scl",
    "ALL",
    "flag",
)

flags = []
